package com.johnnytv.player

/*
 * NOT IN USE.
 *
 * An earlier, simpler recorder lived in this file before RecorderService,
 * RecordScheduler and RecordByTime took the job over between them. The file is
 * left here, emptied, only because the build tree keeps whatever it has been
 * given: nothing refers to it, nothing runs from it, and it can be deleted from
 * the repository whenever somebody is in there anyway.
 */
